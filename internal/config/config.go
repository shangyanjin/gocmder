package config

// Config manages application configuration (YAML)
type Config struct {
	// TODO: Implement config manager
}

// NewConfig creates a new Config instance
func NewConfig() *Config {
	return &Config{}
}
